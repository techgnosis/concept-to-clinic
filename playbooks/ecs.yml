---
- hosts: localhost
  tasks:
  - name: create ECS cluster
    cloudformation:
      stack_name: c2c-ecs
      state: present
      region:
      changeset_name: c2c-ecs
      create_changeset: true
      template: "../templates/ecs.yml"
